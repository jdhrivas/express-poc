version: 2
jobs:
  build:
    docker:
      — image: circleci/node:7.10
    working_directory: ~/repo

    branches:
      only:
        - master
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            npm install
      - run:
          name: Test
          command: |
            npm test